"""Local-only rover exploration simulator.

This package intentionally keeps privileged map/pose state out of agent
observations and rewards. Ground truth is exposed only through `info` for
evaluation.
"""
